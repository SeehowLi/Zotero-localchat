# 更新记录

## 0.4.10 · 2026-09-20

本次发布汇总自 GitHub 上一个版本 0.4.4 以来的改动，包含本地测试版本 0.4.5–0.4.10。

### 阅读与界面

- Local Chat 使用独立的阅读器侧栏面板，不再嵌入论文信息的滚动区域。点击右侧 ChatGPT 图标打开，点击“论文信息”或原生信息/笔记图标返回 Zotero。
- 修正 ChatGPT 图标在侧栏中的尺寸，提供浅色和深色图标；图标归属说明随插件分发。
- 聊天区域随侧栏宽度和窗口高度重新排版，保留用户设置的字号。
- 新增“明亮 / 黑暗 / 跟随 Zotero”主题，自动保存选择；跟随模式通过本机事件连接实时同步。
- 提问使用独立底色、加粗文字、边框和“你的提问 · 编号”，与回答明显区分。
- 新增右侧提问导航：数字快速跳转，展开后显示问题摘要。相同文字的不同提问仍有独立入口。

### 回复格式、速度与滚动

- 保留 App 已渲染正文的标题、列表、引用、表格、代码、链接及公式结构，替代原先的纯文本转发。
- 公式使用浏览器原生 MathML；文件引用以紧凑卡片显示。转发内容经过允许列表过滤，不执行原页面脚本或任意样式。
- 缓存未变化的历史消息，传输变化的消息并复用已有 DOM 节点，减少重复序列化、传输和绘制。
- 发送后只定位一次到本轮问题，后续回复增长不再自动向下滚动。手动滑到底部或点击“最新回复”不会重新开启自动跟随。
- 点击发送或按回车后立即显示提问并清空输入框，无需等待回答完成；等待期间新写的草稿不会被上一条消息的完成事件清除。
- 发送失败时保留本机备份，输入框为空时恢复原消息，不覆盖新草稿，也不自动重复发送。

### 验证与贡献

- 15 项自动化测试通过，覆盖格式、公式、内容过滤、增量更新、滚动、导航、主题、草稿恢复、附件及断线恢复。
- 已在指定版本的正式 Zotero 配置中验证安装、独立面板、主题联动、真实回复格式及提问跳转。未将真实论文或聊天记录加入发布包。
- 将 ChatGPT 列为 AI 辅助贡献者，详见 [贡献者名单](CONTRIBUTORS.md)。

### 升级说明

此更新同时修改了 Zotero 插件和本地中转的前端文件。已有用户请在当前回答完成后运行旧目录的 `Stop-LocalChat.cmd`，解压新的 **Windows 完整包** 到固定位置，运行其中的 `Start-LocalChat.cmd`，并在 Zotero 插件管理器安装包内的 `Zotero-localchat-0.4.10.xpi`。仅升级 XPI 不能更新旧目录中的聊天前端。

聊天关联与运行配置仍保存在 `%LOCALAPPDATA%\ZoteroLocalChat`，升级无需删除该目录，也无需重新上传已关联论文。当前仍为 Windows 预览版；已验证版本与性能范围见 [验证记录](docs/VALIDATION.md)。本地传输优化不等于缩短 ChatGPT 服务端思考、生成或首次 PDF 处理的时间。
